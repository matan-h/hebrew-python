יבא בנוי.ממשק בתור מ # "מ" עכשיו יהיה קיצור ל"ממשק

מ.סיגנון("זהב")

תבנית = [
[מ.טקסט("מה שמך ?"[::-1],מפתח = "-מה-השם-")],
[מ.תיבת_טקסט(מפתח = "-שם-")],
[מ.כפתור("שלח"[::-1])]
]
חלון = מ.חלון("יופי של חלון",תבנית)

בעוד אמת:# לתמיד
    אירוע, ערכים = חלון.קרא()
    אם אירוע הוא כלום: # החלון נסגר
        צא
    אחרת:
        חלון["-מה-השם-"].עדכן(" שלום "[::-1]+ערכים["-שם-"][::-1])
חלון.סגור()